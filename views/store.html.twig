{% extends 'base.html.twig' %}

    {% block page %}
    <div class="col-md-6">
        <h1>{{ stores.getName }}</h1>
        <h2>Update store in the database</h2>
        <form class action="/patch/{{ stores.getId }}" method="post">
            <input name="_method" value="patch" hidden>
            <label for="name">Name:</label>
            <input type="text" name="name" required>
            <button class="btn btn-success" type="submit"> Add </button>
        </form>
        <a href="/store/{{ stores.getId }}">click me</a>
        <br>
        <br>
        <h2>Delete this store</h2>
        <form action="/delete/store/{{ stores.getId }}" method="post">
            <input name="_method" value="delete" hidden>
            <button class="btn btn-danger" type="submit">Delete</button>
        </form>
    </div>
    <div class="col-md-6">
        <h2>Select a brand to add to store:</h2>
        <form action="/add/brand/store" method="post">
            <select name="brand_id">
                <option></option>
                {% if brands is not empty%}
                {% for brand in brands %}
                <option value='{{ brand.getId }}'>{{ brand.getName }}</option>
                {% endfor %}
                {% endif %}
            </select>
            <button class="btn btn-success" type="submit"> Add </button>
        </form>
    </div>
{% endblock %}
